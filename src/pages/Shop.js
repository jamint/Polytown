import { connect } from 'react-redux';
import {
    incrementAction,
    decreaseAction,
} from '../store/actions';

function Shop(props) {
    return (
        <div>Shop</div>
    );
}

export default Shop;